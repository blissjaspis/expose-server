services:
  expose:
    image: jaspis/expose-server:v1.0
    name: expose-server
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - 8080:${PORT}
    environment:
      port: ${PORT}
      domain: ${DOMAIN}
      username: ${ADMIN_USERNAME}
    restart: always
    volumes:
      - ./database/expose.db:/root/.expose
